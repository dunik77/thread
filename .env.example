# Copy to .env and fill in your own token. Never committed -- .env is in .gitignore.
#
# Free token: https://account.bitquery.io/user/api_v2/access_tokens
# Used only by scripts/deployer-history.js, server-side. lookup/index.html needs no key.
BITQUERY_API_TOKEN=
